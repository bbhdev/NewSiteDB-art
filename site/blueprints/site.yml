title: Site

sections:
  # v0.10.39 — the former "Dev tools" dashboard info-section moved to
  # the Panel left-sidebar menu (config.php → panel.menu), the proper
  # home for navigation. See that config block for the rationale.
  pages:
    type: pages
